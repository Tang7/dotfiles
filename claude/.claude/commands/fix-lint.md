# /project:fix-lint

Run the linter and fix all reported issues.

!npm run lint 2>&1

Fix every error listed above. Follow the rules in `rules/code-style.md`.
Do not suppress warnings with eslint-disable comments — fix the root cause.
